---
name: optimization-benchmark
description: Compare original and patched repository behavior and AI usage.
---
# Optimization benchmark
Run the baseline first, then the isolated patch. Track calls, tokens, cost, latency, tests, and failures. Lower confidence when no adequate regression tests exist.
