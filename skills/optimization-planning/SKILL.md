# Optimization planning

Build a dependency-aware plan from evidence-backed candidates. Rank with the
explainable deterministic score: expected savings × confidence × safety × call
frequency × coverage ÷ complexity. Apply one safe step at a time, rerun tests
and evaluations, and rebase remaining assumptions after each accepted step.
